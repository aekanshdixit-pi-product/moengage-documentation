responses:
    # ... other responses
    TooManyRequests:
      description: |
        The following headers are returned in case of rate-limit breach:
        * `x-ratelimit-limit` (integer) - The maximum number of requests that the consumer is permitted to make in a given time window.
        * `x-ratelimit-remaining` (integer) - The number of requests remaining in the current rate limit window.
        * `x-ratelimit-reset` (integer) - The time at which the current rate limit window resets in UTC epoch seconds.
        
        There won't be any response body for this request.
      headers:
        x-ratelimit-limit:
          schema:
            type: integer
          description: The maximum number of requests that the consumer is permitted to make in a given time window.
        x-ratelimit-remaining:
          schema:
            type: integer
          description: The number of requests remaining in the current rate limit window.
        x-ratelimit-reset:
          schema:
            type: integer
          description: The time at which the current rate limit window resets in UTC epoch seconds.
      content:
        # Although the body is empty, defining a content type and an empty example
        # often forces Mintlify to render the dropdown selector.
        application/json:
          schema:
            type: object
            description: No response body is returned for this status code; see headers for details.
          example: null # Use an explicit null example to force the dropdown appearance