openapi: 3.0.3
info:
  title: MoEngage Push API
  version: '2.0'
  description: |
    API for sending transactional and targeted push notifications to users across Android, iOS, and Web platforms.
tags:
  - name: Push
    description: Operations to create and send push notification campaigns.
servers:
  - url: 'https://pushapi-0{dc}.moengage.com/v2'
    description: MoEngage Push API Server
    variables:
      dc:
        default: '1'
        description: "Your MoEngage Data Center (DC). MoEngage hosts each customer in a different DC. You can find your DC number (value of X) and replace the value of 'X' in the URL."
        enum:
          - '1'
          - '2'
          - '3'
          - '4'
security:
  - basicAuth: []
    apiKeyAuth: []

paths:
  /transaction/sendpush:
    post:
      tags:
        - Push
      summary: Send Push Notification
      operationId: sendPushNotification
      description: |
        ### Overview
        The Push API is used to:
        * Create campaigns (targeting all users or a group of users) to send notifications.
        * Target a single user using a unique user attribute.
        * Personalize payload for each user.

        <Note>
          **Rate Limit**: You can create up to **10,000 requests per minute**.
        </Note>

        ### API Endpoint
        The endpoint URL follows the format: `https://pushapi-0X.moengage.com/v2/transaction/sendpush`.
        
        The **'X'** in the API Endpoint URL refers to the MoEngage Data Center (DC). MoEngage hosts each customer in a different DC. You can find your DC number (value of X) and replace the value of 'X' in the URL by referring to the DC and API endpoint mapping [here](https://help.moengage.com/hc/en-us/articles/360057030512-Data-Centers-in-MoEngage#01G5DQVXGT2KZMXTJPF77QPJ25).

        ### Authentication
        The API request will be authenticated through Basic Authentication. Basic Authentication sends a Base64-encoded string containing your username and password with every API request. It encodes a 'username: password' string in Base64 and appends the encoded string with 'Basic '. This string is included in the authorization header as shown below:

        `{"Authorization":"Basic Base64_ENCODED_APPKEY_APIKEY=="}`

        <Info>
        **How to obtain credentials:**
        1. On the left navigation menu in the MoEngage dashboard, go to **Settings > Account > APIs**.
        2. Copy the following details:
           * **Username**: Under **Workspace ID (earlier app id)**, click the copy icon to copy the username.
           * **Password**: In the **API keys** section, click the copy icon in the **Push templates** tile to copy the API key.
        3. Use these details to authenticate the API requests.
        </Info>

        ### FAQs
        <AccordionGroup>
          <Accordion title="Does the Push API support silent notifications?">
            No, MoEngage Push API doesn't support silent notifications.
          </Accordion>
          <Accordion title="Can I use HTML to format notification text?">
            Yes, the basic template supports HTML tags.
          </Accordion>
        </AccordionGroup>

      requestBody:
        required: true
        description: The complete configuration for the push campaign.
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PushRequest'
            examples:
              TargetSingleUser:
                summary: Target Single User
                value:
                  appId: "ABC123_DEBUG"
                  campaignName: "Test_Campaign_Single_User"
                  signature: "{{Signature}}"
                  requestType: "push"
                  targetPlatform: ["ANDROID"]
                  targetAudience: "User"
                  targetUserAttributes:
                    attribute: "USER_ATTRIBUTE_USER_EMAIL"
                    comparisonParameter: "is"
                    attributeValue: "abc@example.com"
                  payload:
                    ANDROID:
                      groupKey: "promo_group"
                      collapseReplaceKey: "promo_update"
                      message: "Welcome to our platform!"
                      title: "Welcome"
                      summary: "Welcome Message"
                      richContent:
                        - type: "image"
                          value: "https://images.example.com/welcome.png"
                      defaultAction:
                        type: "richlanding"
                        value: "https://www.moengage.com"
                  campaignDelivery:
                    type: "soon"
                  conversionGoals:
                    - name: "Purchase"
                      eventName: "PurchaseSuccess"
                      attrs:
                        name: "price"
                        filter: "is greater than"
                        value: "100"
                        type: "numeric"
                  advancedSettings:
                    ttl:
                      ANDROID: 12
                    ignoreFC: false
                    sendAtHighPriority: true
              TargetAllUsers:
                summary: Target All Users (Multi-platform)
                value:
                  appId: "ABC123_DEBUG"
                  campaignName: "Global_Announcement"
                  signature: "{{Signature}}"
                  requestType: "push"
                  targetPlatform: ["ANDROID", "IOS", "WEB"]
                  targetAudience: "All Users"
                  payload:
                    ANDROID:
                      message: "Big Sale Starts Now!"
                      title: "Flash Sale"
                      defaultAction:
                        type: "richlanding"
                        value: "https://www.example.com/sale"
                    IOS:
                      message: "Big Sale Starts Now!"
                      title: "Flash Sale"
                      richContent:
                        - type: "sound"
                          value: "default"
                    WEB:
                      message: "Big Sale Starts Now!"
                      title: "Flash Sale"
                      redirectURL: "https://www.example.com/sale"
                      iconURL: "https://www.example.com/icon.png"
                  campaignDelivery:
                    type: "soon"
              TargetCustomSegment:
                summary: Target Custom Segment
                value:
                  appId: "ABC123_DEBUG"
                  campaignName: "Re-engagement Campaign"
                  signature: "{{Signature}}"
                  requestType: "push"
                  targetPlatform: ["ANDROID"]
                  targetAudience: "Custom Segment"
                  customSegmentName: "Inactive_Users_30_Days"
                  payload:
                    ANDROID:
                      message: "We miss you! Come back for 20% off."
                      title: "We miss you"
                      defaultAction:
                        type: "richlanding"
                        value: "https://www.example.com/comeback"
                  campaignDelivery:
                    type: "soon"

      responses:
        '200':
          description: |
            This response is returned when the request is processed successfully.
            
            **Common Error Responses:**
            * `Invalid JSON`: Returned when an incorrect JSON format is passed.
            * `Invalid Request Type`: Returned when an invalid request type is passed (other than *push*).
            * `Invalid appId`: Returned when no dashboard exists for the given app ID.
            * `appId is not present`: Returned when appID is missing in the request.
            * `Campaign name is missing`: Returned when the campaign name is missing in the request.
            * `Signature is not verified`: Returned when the signature is incorrect.
          content:
            application/json:
              schema:
                oneOf:
                  - $ref: '#/components/schemas/PushSuccessResponse'
                  - $ref: '#/components/schemas/PushFailureResponse'
              examples:
                success:
                  summary: Success Response
                  value:
                    requestId: "06418699-2534-4f53-b297-8c1489948382"
                    responseId: "5f7c3b2a-1e4d-4a9c-8b2e-7d1f6a5c9e3d"
                    cid: "29384756"
                    status: "Success"
                failure:
                  summary: Failure Response
                  value:
                    responseId: "5f7c3b2a-1e4d-4a9c-8b2e-7d1f6a5c9e3d"
                    Error_Message: "Campaign Name not found"
                    status: "Fail"
                    Payload error: "Campaign Name not found"
        '401':
          description: Unauthorized. Invalid authentication credentials.

components:
  securitySchemes:
    basicAuth:
      type: http
      scheme: basic
      description: Basic Authentication using Workspace ID as username and Push API Key as password.
    apiKeyAuth:
      type: apiKey
      in: header
      name: MOE-APPKEY
      description: |
        This is the workspace ID (earlier APP ID) of your MoEngage workspace. The MOE-APPKEY must be passed in the request. You can find your MoEngage Workspace ID in the MoEngage dashboard: **Settings > Account > APIs -> Workspace ID (earlier app id)**.

  schemas:
    # --- Main Request Schema ---
    PushRequest:
      type: object
      required:
        - appId
        - campaignName
        - signature
        - targetPlatform
        - targetAudience
        - payload
        - campaignDelivery
      properties:
        appId: 
          type: string
          description: "This field denotes the app ID that you must pass to create the campaign. You can find it by selecting **Settings > Account > APIs -> Workspace ID (earlier app id)** on the MoEngage dashboard. MoEngage recommends that you test API integration in the Test Environment. The App ID for TEST ACCOUNT is suffixed with `_DEBUG`."
        campaignName: 
          type: string
          minLength: 5
          description: |
            This field denotes the campaign name. The campaign name should be unique for *All Users* and *Custom Segment* campaigns.
            
            **Note**:
            * API automatically creates a campaign with the given name. You need not create a campaign from the dashboard to use the name.
            * For User type *targetAudience* API requests, reuse this campaign name for requests that should be clubbed in the same campaign.
        campaignTags: 
          type: array
          description: |
            Used to add more context to campaigns. Context can be descriptive names or campaign themes such as onboarding, retention, country, and all users. Tags can also be used to filter campaigns or identify the behaviors of users who received these campaigns. The list of tags should be passed in this field.
            
            For example, `“campaignTags“ : [“Transactional“, “Accounts“]`
          items: { type: string }
        signature: 
          type: string
          description: |
            A signature is required to verify whether the client is authorized to make the API call. It is generated from a hash of the campaign name, app ID, and API key. MoEngage uses the SHA 256 method to generate this hash.
            
            **Python 3.x Generation:**
            ```python
            from hashlib import sha256
            app_id = "XXXXXXXXXXXXXXXXXXXXXX_DEBUG" # (Please refer to required parameters section)
            campaign_name = "TEST"
            api_key = "XXXXXXXXXXXX" # This is the sample key. Each app has a different API key
            signature_key = app_id+'|'+ campaign_name+'|'+ api_key
            signature = sha256(signature_key.encode('utf-8')).hexdigest()
            print(signature)
            ```
        requestType: 
          type: string
          default: "push"
          description: "The value of this key is used to indicate the intention of an API request. The default value is *push*."
        targetPlatform:
          type: array
          description: "This field denotes the platform devices that are targeted by your campaign. The Push API allows you to target users on Android, iOS, and Web platforms. You can target one or more platforms. If you want to send Web (Chrome) push notifications through the API, you should choose *Web* as the platform."
          items:
            type: string
            enum: [ANDROID, IOS, WEB]
        targetAudience:
          type: string
          description: |
            This field denotes the target segment for the campaign. Supported values are:
            * `All Users`: Pass this value to target all users of your App/Website.
            * `Custom Segment`: Pass this value to target pre-created user segments. Custom segments can be created using the MoEngage dashboard or custom segment API. Note that the custom segment name should exactly match the name of the target segment.
            * `User`: Pass this value to target a user with an attribute. It will target a maximum of 100 users of the associated **targetUserAttributes** definition.
          enum: ["All Users", "Custom Segment", "User"]
        customSegmentName:
          type: string
          description: "Required if `targetAudience` is **Custom Segment**. The custom segment name should exactly match the name of the target segment."
        targetUserAttributes:
          $ref: '#/components/schemas/TargetUserAttributes'
        payload:
          $ref: '#/components/schemas/Payload'
        campaignDelivery:
          $ref: '#/components/schemas/CampaignDelivery'
        conversionGoals:
          type: array
          maxItems: 5
          description: |
            This object contains the conversion goals details.
            * You can pass a single conversion goal or multiple conversion goals.
            * If you are passing multiple conversion goals, the first conversion goal in the list is considered the primary conversion goal for the campaign.
            * A Push campaign supports a maximum of five conversion goals on its list.
          items:
            $ref: '#/components/schemas/ConversionGoal'
        advancedSettings:
          $ref: '#/components/schemas/AdvancedSettings'

    # --- Main Response Schemas ---
    PushSuccessResponse:
      type: object
      properties:
        status:
          type: string
          enum: [Success]
          description: "This field indicates the campaign status."
        requestId: 
          type: string
          description: "This field indicates the request ID."
        responseId: 
          type: string
          description: "This field indicates the response ID."
        cid: 
          type: string
          description: "This field indicates the campaign ID."

    PushFailureResponse:
      type: object
      properties:
        status:
          type: string
          enum: [Fail]
          description: "This field indicates failure."
        Error_Message: { type: string, example: "Campaign Name not found" }
        "Payload error": { type: string, example: "Campaign Name not found" }
        responseId: { type: string }
        requestId: { type: string }

    # --- Support Schemas ---
    TargetUserAttributes:
      type: object
      description: |
         This object contains the target user attribute details.
         
         **Note**:
         * This value is required when the **targetAudience** value is *User*.
         * For API requests where the target audience is a specific user, if a campaign with the given name already exists, existing campaign data corresponding to the given name will be used to send notifications.
         * Any edits in other parts of campaign data except payload within the API request parameters will be ignored.
         * If any changes are required in the campaign data, change the campaign name in the API request to create a new campaign.
      properties:
        attribute:
          type: string
          description: |
            This field denotes a unique identifier or characteristic of an entity or object used to specify the target users. This value is required when the **targetAudience** value is *User*.
            
            Other fields used are:
            * for unique ID: `USER_ATTRIBUTE_UNIQUE_ID`
            * for email: `USER_ATTRIBUTE_USER_EMAIL`
            * For mobile number: `USER_ATTRIBUTE_USER_MOBILE`
            * for Google Advertising identifier: `MOE_GAID`
            * for iOS Identifier for advertising: `ADVERTISING_IDENTIFIER`
            * for device push token: `PUSH_ID` (the same key is used for Android GCM ID, iOS push token)
          enum: [USER_ATTRIBUTE_UNIQUE_ID, USER_ATTRIBUTE_USER_EMAIL, USER_ATTRIBUTE_USER_MOBILE, MOE_GAID, ADVERTISING_IDENTIFIER, PUSH_ID]
        comparisonParameter:
          type: string
          description: |
            This field denotes a condition or operator that compares the user attributes against a specific value. Supported values are:
            * The "is" query type
            * The "in" query type
            
            **Note**: The maximum number of parameters that can be added while using the "in" comparisonParameter is 50. This value is required when the **targetAudience** value is *User*.
          enum: [is, in]
        attributeValue:
          oneOf:
            - type: string
            - type: array
              items:
                type: string
          description: "This field denotes the actual value assigned to an attribute, which is used for comparison or filtering. This value is required when the **targetAudience** value is *User*."
        isCustom:
          type: boolean
          description: "This field denotes whether a custom attribute has been added. For custom attributes, targeting is done on any custom attribute with the available fields. Set the key 'isCustom' to true."
          default: false

    DefaultAction:
      type: object
      description: "This helps you define the default action triggered when the user interacts with the notification. Supported values: `richlanding`, `navigation`, `deeplinking`."
      properties:
        type:
          type: string
          enum: [richlanding, navigation, deeplinking]
        value:
          type: string
          description: "The URL or deep link path."
        kvPairs:
          type: object
          additionalProperties:
            type: string
          description: "Additional key-value pairs to pass with the action."

    AndroidPayload:
      type: object
      required: [message, title]
      properties:
        groupKey: 
          type: string
          description: |
            This field denotes the group key used to identify and categorize related push notifications.
            
            **Note**:
            * Ensure you use the same group key for all push notifications you want to group.
            * MoEngage automatically modifies the group key to ensure it does not exceed the 45-character limit. MoEngage removes non-Latin scripts, special characters, and spaces to restrict the key composition to ASCII characters exclusively.
        collapseReplaceKey: 
          type: string
          description: "This field denotes the update key used to identify and update related push notifications. Ensure you use the same update key for all push notifications intended to update each other."
        message: 
          type: string
          description: "This field denotes the main content or text to be displayed in the notification/message."
        title: 
          type: string
          description: "This field denotes the headline or subject of the notification/message."
        channel_id:
          type: string
          description: "This field denotes the channel identifier for the campaign."
        summary: 
          type: string
          description: "This field denotes the brief summary or condensed version of the content. Typically displayed in collapsed notifications."
        richContent:
          type: array
          description: |
            An object or HTML string containing rich text, images, or other multimedia to enhance the message. Supported values are:
            * Coupon
            * Image
            * Sound
          items:
            type: object
            properties:
              type: { type: string, enum: [image, coupon, sound] }
              value: { type: string }
        defaultAction:
          $ref: '#/components/schemas/DefaultAction'
        additionalActions:
          type: array
          description: |
            This object contains a list of extra actions or buttons that the user can perform, providing more functionality for the notification. Supported actions are:
            * Call
            * Snooze
            * Copy
            * Share
            
            A maximum of two additional action buttons can be used at a time.
          maxItems: 2
          items:
            type: object
            properties:
              type: { type: string, enum: [call, snooze, copy, share] }
              value: { type: string }
              name: { type: string }
              iconURL: { type: string }
        fallback: 
          type: object
          description: "This JSON object contains the fallback values for the values mentioned above."

    IOSPayload:
      type: object
      required: [message, title]
      properties:
        groupKey: 
          type: string
          description: |
            This field denotes the group key used to identify and categorize related push notifications.
            
            **Note**:
            * Ensure you use the same group key for all push notifications you want to group.
            * MoEngage automatically modifies the group key to ensure it does not exceed the 45-character limit. MoEngage removes non-Latin scripts, special characters, and spaces to restrict the key composition to ASCII characters exclusively.
        collapseReplaceKey: 
          type: string
          description: "This field denotes the update key used to identify and update related push notifications. Ensure you use the same update key for all push notifications intended to update each other."
        message: 
          type: string
          description: "This field denotes the primary text or body of the notification/message."
        title: 
          type: string
          description: "This field denotes the title or subject line of the notification/message."
        subtitle: 
          type: string
          description: "This field denotes the subtitle of the notification/message."
        enableIosBadge: 
          type: boolean
          description: "This field specifies whether a notification should automatically update the iOS app’s badge count with push notifications. Supported values are 'true' and 'false'."
        richContent:
          type: array
          description: |
            This field denotes a resource, such as an image or multimedia attachment, to provide additional visual or interactive elements. Supported contents are:
            * GIF
            * Coupon
            * Sound
            * Image
            * Audio
            * Video
            
            Image, video, and audio are only supported after iOS-10.
            
            **Note**: Image, video, and audio can not be used together.
          items:
            type: object
            properties:
              type: { type: string, enum: [gif, coupon, sound, image, audio, video] }
              value: { type: string }
        defaultAction:
          description: |
            This object contains a set of default actions associated with the notifications. Supported values are:
            * Navigation
            * Richlanding
            * Deeplinking
          $ref: '#/components/schemas/DefaultAction'
        additionalActions:
          type: array
          description: "This object contains a set of supplementary actions or buttons associated with the notification, enabling users to perform specific tasks. You can add only one category for additional actions."
          items:
            type: object
            properties:
              category: { type: string }
        fallback: 
          type: object
          description: "This JSON object contains the fallback values for the values mentioned above."

    WebPayload:
      type: object
      required: [message, title, redirectURL, iconURL]
      properties:
        message: 
          type: string
          description: "This field denotes the main text content or notification body displayed to users."
        title: 
          type: string
          description: "This field denotes the subject or heading of the notification."
        redirectURL: 
          type: string
          format: uri
          description: "This field denotes the URL to which the user will be redirected upon interacting with the notification."
        iconURL: 
          type: string
          format: uri
          description: "This field denotes the URL of an icon image representing the notification, usually displayed alongside the title and message."
        imageURL: 
          type: string
          format: uri
          description: "This field denotes the URL of an additional image to be shown in the notification, providing further context or visual appeal."
        autoDismiss: 
          type: boolean
          description: "This field specifies whether to dismiss the notification automatically after 8 seconds."
        fallback: 
          type: object
          description: "This JSON object contains the fallback values for the values mentioned above."

    Payload:
      type: object
      description: "This object contains the payload details. Supported values are Android, iOS, and Web."
      properties:
        ANDROID:
          $ref: '#/components/schemas/AndroidPayload'
        IOS:
          $ref: '#/components/schemas/IOSPayload'
        WEB:
          $ref: '#/components/schemas/WebPayload'

    CampaignDelivery:
      type: object
      required: [type]
      properties:
        type:
          type: string
          description: |
             This field helps decide when the campaign should be sent.
             
             **Note**:
             * All notifications targeted to a single user will be sent as soon as possible (ASAP).
             * Scheduling campaigns for Later or Periodic is applied only for segmented or *All users* campaigns.
             * The **timezone** field can be used to pass the timezone for scheduled campaigns.
          enum: [soon, later, periodic]
        date:
          type: string
          description: "Required for 'later' type. Format: `MM/DD/YYYY`"
        time:
          type: string
          description: "Required for 'later' and 'periodic' types. Format: `HH:MM AM/PM`"
        endDate:
          type: string
          description: "Required for 'periodic' type. Format: `MM/DD/YYYY`"
        repeatAfter:
          type: string
          description: "Repeat interval in days for 'periodic' type."
        timezone:
          type: string
          description: "Timezone for scheduling (e.g., 'Asia/Kolkata'). Defaults to IST if not provided."

    ConversionGoal:
      type: object
      required: [name, eventName]
      properties:
        name: { type: string, description: "This field denotes the readable name of the conversion goal for the Campaign Info." }
        eventName: { type: string, description: "This field denotes the actual event name." }
        attrs:
          type: object
          description: "This field denotes the event attribute filters."
          properties:
            name: 
              type: string
              description: "Event attribute name. This is mandatory."
            filter: 
              type: string
              description: |
                Event attribute filter operator. This is mandatory.
                * **String Operators:** is, is not, contains, does not contain, starts with, ends with, exists, etc.
                * **Numeric Operators:** is equal to, is not equal to, is between, is less than, is greater than, exists, etc.
            value: 
              oneOf: [{ type: string }, { type: number }, { type: array, items: { type: string } }]
              description: "Event attribute filter value."
            type: 
              type: string
              enum: [string, numeric]
              description: "Event attribute data type. This is mandatory."

    AdvancedSettings:
      type: object
      description: "This object contains information about advanced settings."
      properties:
        ttl:
          type: object
          description: "This field denotes the duration for which the push notification service keeps a push message before it's discarded if it can't be delivered. If not delivered, Push Services (GCM/WEB) retry to send the message until the TTL (in minutes) does not expire. Send ‘0’ if it is not applicable to you. The default value for TTL is 36 hours and applies only to Android push or Web push notifications."
          properties:
            ANDROID: { type: integer }
            WEB: { type: integer }
        ignoreFC:
          type: boolean
          description: |
            This field denotes if the ignoreFC is enabled. Supported values are:
            * `true`: Bypasses frequency capping set on the dashboard and sends the notifications to everyone in the target segment.
            * `false`: Applies the frequency capping set on the dashboard and does not send the notification after the limit is reached.
            
            **Note**: This field is case-sensitive.
        countFC:
          type: boolean
          description: |
            This field denotes if the countFC is enabled.
            * `true`: Only Push campaigns are counted in the frequency cap count.
            * `false`: Push campaigns are not counted in the frequency cap count.
            
            Values are "true" or "false" only when ignoreFC value is set to true.
        notificationPrioritySettings:
          type: object
          description: "This field denotes whether the notification priority settings are configured. Supported values are 'true' or 'false' only for Android push / Web push notifications. It uses the GCM/FCM flag to send time-critical messages with high priority and drains the user's device battery more than normal messages."
          properties:
            Android:
              type: string
              enum: [High, Low]
            iOS:
              type: integer
              enum: [1, 5, 10]
        "pushAmp+":
          type: boolean
          description: "This field denotes whether the pushAmp+ is enabled. Supported values are 'true' or 'false'."
        preCacheSegment:
          type: boolean
          description: "This field denotes whether the preCacheSegment is enabled. Supported values are 'true' or 'false.' Applicable for scheduled and periodic campaigns. If enabled, pre-caches the segmentation and improves campaign sending time. For caching, it is recommended that the campaign be created at least 2 hours before the scheduled time."
        campaignThrottleTime:
          type: integer
          description: "This field denotes if the campaign throttle time is configured. Push throttling allows you to control the rate of sending a push notification. The value to be passed can be between 1 and 360 (minutes)."
        campaignThrottleRpm:
          type: integer
          description: |
            The default throughput for the Push API is 100K RPM. You can send either of the key-value pairs in the Push API payload between campaignThrottleRpm and campaignThrottleTime.
            * If you send both, the value for campaignThrottleRpm takes precedence.
            * If you do not send any of them, the value defined in settings is considered.
        useJinjaPersonalization:
          type: boolean
          description: "This field denotes whether Jinja personalization can be used in the payload. Values are 'true' or 'false.' It is recommended that this personalization format be used. For example: `title: Hello {{UserAttribute['userName']}}`."
        ignoreMinimumDelay:
          type: boolean
          default: true
          description: "This field denotes whether *ignore minimum delay* is enabled. Supported values are 'true' or 'false.' This is a boolean value to indicate if the Client level Minimum Delay setting should be applied for the campaign. The default value is true, if this field is not passed in the API."
        inboxExpiry:
          type: integer
          description: "This field specifies the expiry value for the push notification in the App Inbox in hours."
        iOSTargetDevice:
          type: string
          description: "Supported values are: `apns_device_default_push`, `apns_device_provisional_push`, `apns_device_non_provisional_push`."
          enum: [apns_device_default_push, apns_device_provisional_push, apns_device_non_provisional_push]
        deliverWhenActive:
          type: boolean
          description: "This field specifies whether the push notifications will be delivered when the device is in an active state. Supported value: 'true' and 'false'."
        bypassDND:
          type: boolean
          description: "This field specifies whether to deliver the notification immediately, bypassing device restrictions such as battery optimizations or low-power modes."