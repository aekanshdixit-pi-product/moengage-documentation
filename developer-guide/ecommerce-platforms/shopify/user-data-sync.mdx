---
title: "Sync Shopify Users and Order History"
description: "Sync your existing Shopify users and order history to MoEngage for segmentation and engagement."
---

After you complete the initial integration, you can sync your existing Shopify users and their historical order data to MoEngage. This process allows you to build segments, trigger campaigns, and personalize communication based on past user behavior from the moment you start using MoEngage.

# Data Synchronized to MoEngage

The synchronization process imports two primary data types into MoEngage:

| Data Type | Description |
| :-- | :-- |
| **Order history** | MoEngage ingests past orders within your selected date range as individual [`Shopify - Order Placed`](https://developers.moengage.com/hc/en-us/articles/18357477009940-Events-and-User-Data-Tracking) events. Each event uses the original order timestamp from Shopify. |
| **User profiles** | MoEngage imports all registered users in your Shopify store. The system creates or updates profiles using the unique identifier (email or mobile number) you selected during integration setup. |

### User Attribute Mapping

MoEngage synchronizes the following attributes to the user profile if they are available in the Shopify record:

- Email
- First name
- Last name
- Phone
- City
- Country
- State
- Shopify ID
- Accepts marketing
- Order count

<Info>
  **What MoEngage Does Not Sync**

  By default, MoEngage does not sync Shopify customer metafields (custom data fields). If your personalization requires metafield data, refer to the [Events and User Data Tracking](https://developers.moengage.com/hc/en-us/articles/18357477009940) article for custom integration steps.
</Info>

# Prerequisites for Synchronization

### Check the Event Retention Period

Before you begin the synchronization process, perform the following step to ensure your event retention settings accommodate the data range you intend to sync:

MoEngage stores events for a specific retention window. If your selected sync range exceeds your retention window, MoEngage stops syncing orders once it reaches the limit. The sync status will show as "Sync successful," but older data will be missing from the dashboard.

<Warning>
  **Warning**

  If you plan to sync more than 60 days of history, contact your Customer Success Manager or raise a support ticket to increase your event retention period **before** starting the sync. You cannot apply these changes retroactively.
</Warning>

### Validate Unique Identifiers

MoEngage synchronizes users and their orders based on the unique identifier you selected during integration setup:

- If you selected **email**, the system silently excludes users who do not have an email address.
- The system does not generate error messages for skipped records.

# Synchronize Your Shopify Data

To perform a historical data backfill, perform the following steps:

1. Open your MoEngage Shopify app and navigate to the **Sync Shopify data** tab.
2. Select the **Date range** for the order history. You can sync data for up to the last 2 years from your installation date.
3. Click **Sync past data**. <img src="/images/SyncPastData.png" alt="Sync Past Data" /> \
   Monitor the progress in the **Sync progress** section.

# Confirm the Data Transfer

<Info>
  **Sync Status Does Not Confirm Data Arrival**

  The sync status may update to **Sync successful** within seconds of starting. However, this only indicates that MoEngage received the request. Processing large datasets can take several hours, and up to 24 hours for very large stores.
</Info>

To confirm that the data transfer completed successfully, perform the following steps:

1. You must wait for two hours after starting the sync.
2. In the MoEngage UI, navigate to a user profile for a customer who placed an order within the sync window.
3. Verify that the `Shopify - Order Placed` event appears on their profile with the correct original date.
4. If the sync status shows as successful but no data appears after 24 hours, contact support with your selected date range and approximate order count.

<Warning>
  **Important**

  The sync is a one-time operation for historical backfill. Once complete, Shopify webhooks track new orders automatically. Do not run the sync a second time for the same date range, as this action creates duplicate events.
</Warning>

The synchronization process imports two primary data types into MoEngage:

| Data Type | Description |
| :-- | :-- |
| **Order history** | MoEngage ingests past orders within your selected date range as individual [`Shopify - Order Placed`](https://developers.moengage.com/hc/en-us/articles/18357477009940-Events-and-User-Data-Tracking) events. Each event uses the original order timestamp from Shopify. |
| **User profiles** | MoEngage imports all registered users in your Shopify store. The system creates or updates profiles using the unique identifier (email or mobile number) you selected during integration setup. |

### User Attribute Mapping

MoEngage synchronizes the following attributes to the user profile if they are available in the Shopify record:

- Email
- First name
- Last name
- Phone
- City
- Country
- State
- Shopify ID
- Accepts marketing
- Order count

<Info>
  **What MoEngage Does Not Sync**

  By default, MoEngage does not sync Shopify customer metafields (custom data fields). If your personalization requires metafield data, refer to the [Events and User Data Tracking](https://developers.moengage.com/hc/en-us/articles/18357477009940) article for custom integration steps.
</Info>

# Prerequisites for Synchronization

### Check the Event Retention Period

Before you begin the synchronization process, perform the following step to ensure your event retention settings accommodate the data range you intend to sync:

MoEngage stores events for a specific retention window. If your selected sync range exceeds your retention window, MoEngage stops syncing orders once it reaches the limit. The sync status will show as "Sync successful," but older data will be missing from the dashboard.

<Warning>
  **Warning**

  If you plan to sync more than 60 days of history, contact your Customer Success Manager or raise a support ticket to increase your event retention period **before** starting the sync. You cannot apply these changes retroactively.
</Warning>

### Validate Unique Identifiers

MoEngage synchronizes users and their orders based on the unique identifier you selected during integration setup:

- If you selected **email**, the system silently excludes users who do not have an email address.
- The system does not generate error messages for skipped records.

# Synchronize Your Shopify Data

To perform a historical data backfill, perform the following steps:

The synchronization process imports two primary data types into MoEngage:

| Data Type | Description |
| :-- | :-- |
| **Order history** | MoEngage ingests past orders within your selected date range as individual [`Shopify - Order Placed`](https://developers.moengage.com/hc/en-us/articles/18357477009940-Events-and-User-Data-Tracking) events. Each event uses the original order timestamp from Shopify. |
| **User profiles** | MoEngage imports all registered users in your Shopify store. The system creates or updates profiles using the unique identifier (email or mobile number) you selected during integration setup. |

### User Attribute Mapping

MoEngage synchronizes the following attributes to the user profile if they are available in the Shopify record:

- Email
- First name
- Last name
- Phone
- City
- Country
- State
- Shopify ID
- Accepts marketing
- Order count

<Info>
  **What MoEngage Does Not Sync**

  By default, MoEngage does not sync Shopify customer metafields (custom data fields). If your personalization requires metafield data, refer to the [Events and User Data Tracking](https://developers.moengage.com/hc/en-us/articles/18357477009940) article for custom integration steps.
</Info>

# Prerequisites for Synchronization

### Check the Event Retention Period

Before you begin the synchronization process, perform the following step to ensure your event retention settings accommodate the data range you intend to sync:

MoEngage stores events for a specific retention window. If your selected sync range exceeds your retention window, MoEngage stops syncing orders once it reaches the limit. The sync status will show as "Sync successful," but older data will be missing from the dashboard.

<Warning>
  **Warning**

  If you plan to sync more than 60 days of history, contact your Customer Success Manager or raise a support ticket to increase your event retention period **before** starting the sync. You cannot apply these changes retroactively.
</Warning>

### Validate Unique Identifiers

MoEngage synchronizes users and their orders based on the unique identifier you selected during integration setup:

- If you selected **email**, the system silently excludes users who do not have an email address.
- The system does not generate error messages for skipped records.

# Synchronize Your Shopify Data

To perform a historical data backfill, perform the following steps:

1. Open your MoEngage Shopify app and navigate to the **Sync Shopify data** tab.
2. Select the **Date range** for the order history. You can sync data for up to the last 2 years from your installation date.
3. Click **Sync past data**. <img src="/images/SyncPastData.png" alt="Sync Past Data" /> \
   Monitor the progress in the **Sync progress** section.

# Confirm the Data Transfer

<Info>
  **Sync Status Does Not Confirm Data Arrival**

  The sync status may update to **Sync successful** within seconds of starting. However, this only indicates that MoEngage received the request. Processing large datasets can take several hours, and up to 24 hours for very large stores.
</Info>

To confirm that the data transfer completed successfully, perform the following steps:

1. You must wait for two hours after starting the sync.
2. In the MoEngage UI, navigate to a user profile for a customer who placed an order within the sync window.
3. Verify that the `Shopify - Order Placed` event appears on their profile with the correct original date.
4. If the sync status shows as successful but no data appears after 24 hours, contact support with your selected date range and approximate order count.

<Warning>
  **Important**

  The sync is a one-time operation for historical backfill. Once complete, Shopify webhooks track new orders automatically. Do not run the sync a second time for the same date range, as this action creates duplicate events.
</Warning>

# Confirm the Data Transfer

<Info>
  **Sync Status Does Not Confirm Data Arrival**

  The sync status may update to **Sync successful** within seconds of starting. However, this only indicates that MoEngage received the request. Processing large datasets can take several hours, and up to 24 hours for very large stores.
</Info>

To confirm that the data transfer completed successfully, perform the following steps:

1. You must wait for two hours after starting the sync.
2. In the MoEngage UI, navigate to a user profile for a customer who placed an order within the sync window.
3. Verify that the `Shopify - Order Placed` event appears on their profile with the correct original date.
4. If the sync status shows as successful but no data appears after 24 hours, contact support with your selected date range and approximate order count.

<Warning>
  **Important**

  The sync is a one-time operation for historical backfill. Once complete, Shopify webhooks track new orders automatically. Do not run the sync a second time for the same date range, as this action creates duplicate events.
</Warning>