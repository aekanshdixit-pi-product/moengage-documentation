---
title: "Why Does an HTML In-App Fail to Display When a URL Is Included Inside a Script Tag?"
description: "Diagnose why an HTML in-app campaign fails to render when a URL is referenced inside a script tag."
---

## Problem

{/* TODO: Describe the rendering failure that occurs when a URL is placed inside a `<script>` tag in an HTML in-app. */}

## Cause

{/* TODO: Explain why the SDK or WebView blocks or fails to load script-tag URLs (for example, mixed content, CSP, sanitization). */}

## Solution

{/* TODO: Provide the recommended way to include external scripts or URLs in an HTML in-app. */}
