---
title: "Olo integration"
description: "Learn how to integrate Olo with MoEngage using Olo's self-service webhook framework to sync restaurant guest data and power personalized, lifecycle-driven campaigns."
keywords: ["Olo", "restaurant webhooks", "guest data", "order events", "webhook integration"]
---

## Introduction

[Olo](https://www.olo.com/) is a restaurant technology platform for multi-unit restaurant brands. Through its self-service webhook framework, Olo emits near-real-time event data — including orders, guest sign-ups, and opt-in preferences — that you can stream directly into MoEngage to power personalized, lifecycle-driven campaigns.

## MoEngage and Olo

With this integration, you can:

- Ingest order events (placed, cancelled, picked up) as MoEngage behavioral events in near real time.
- Sync guest sign-up and opt-in data to create and enrich MoEngage user profiles.
- Capture loyalty check-ins and vendor-level events for advanced segmentation.
- Trigger personalized campaigns immediately after an order is placed or a guest churns.

## Use cases

The following use cases illustrate how to activate Olo data in MoEngage:

- **Re-order campaigns**: Trigger personalized push or email campaigns when a guest's last order crosses a defined recency threshold.
- **New guest onboarding**: Welcome guests who sign up via Olo ordering flows and deliver first-order incentive campaigns.
- **Win-back lapsed guests**: Identify users with no **Order Placed** events in 30+ days and serve re-engagement campaigns with relevant offers.
- **Order milestone rewards**: Detect guests reaching their 5th or 10th lifetime order and trigger loyalty reward messages automatically.
- **Post-purchase feedback**: Send automated review prompts or satisfaction surveys within minutes of an **Order Picked Up** event.

The following table lists the main Olo webhook events. Additional events can be configured based on your use case.

| Olo event type | Description |
|---|---|
| `UserSignedUp` | Guest signs up via Olo. Profile attributes and subscription preferences are captured. |
| `OrderPlaced` | Guest places an order. Basket contents, location, totals, and delivery method are captured. |
| `OrderCancelled` | Guest cancels an order. Used in win-back flows and order analysis. |
| `OrderPickedUp` | Guest collects their order. Triggers post-purchase flows. |
| `GuestOptIn` | Guest updates their communication preferences. Email and SMS subscription state is captured. |

## Prerequisites

Before you begin, ensure you have the following:

- **Brand Manager** permission (or higher) with access to **Dev Tools > Webhooks** in the Olo Dashboard.
- Your MoEngage **Workspace ID** and **Data API Key**, available under **Settings > Account > APIs**.

<Info>
Olo allows only one Destination URL per brand environment. All event types are routed to the same MoEngage endpoint and differentiated using the `X-Olo-Event-Type` header.
</Info>

## Step 1: Get your MoEngage endpoint

MoEngage provides a dedicated webhook ingestion endpoint for this integration. Use this URL as the **Destination URL** in the Olo webhook configuration.

The MoEngage endpoint follows this format:

```text
https://api-0X.moengage.com/v1/partner/olo/{events|user}/?configName={config_name}
```

## Step 2: Configure the webhook in the Olo Dashboard

<Steps>
<Step title="Open the Webhooks section">
In the Olo Dashboard, navigate to **Dev Tools > Webhooks** in the left navigation.

<Note>MFA must be enabled on your Olo account to access this section.</Note>
</Step>

<Step title="Add a webhook">
Click **Add Webhook** in the top-right corner.

{/* TODO: Replace with a MoEngage-hosted screenshot. The image below is from a third-party site and may break. */}
<Frame>
  <img src="https://docs.bluedot.io/assets/images/Add-webhook-from-olo-1024x155-1470439a688550ea01034b79caeeac6c.png" alt="Olo Dashboard showing the Add Webhook button in the top-right corner" />
</Frame>
</Step>

<Step title="Configure the webhook settings">
Fill in the following fields:

- **Webhook Name**: Enter a descriptive name, for example: `MoEngage - Your Workspace Name`.
- **Destination URL**: Paste the MoEngage endpoint URL from Step 1.
- **Developer Partner**: Select **MoEngage** from the dropdown.
</Step>

<Step title="Add the Authorization header">
Under **Headers**, add an `Authorization` header with a Basic Authentication token.

To generate the token:

1. Navigate to **Settings > Account > APIs** in MoEngage.
2. Copy your **Workspace ID** (also labeled as **app id** in older workspaces).
3. Copy the **Data** value under **API keys**.
4. Encode the credentials using the format `WorkspaceID:DataAPIKey` in Base64. For example: `Basic Qk5CR1NNNUR4eHh4eHhWQ1hSRFJEWTdSOlFsWHVoTHVaeHh4eHh4QWQrQnBYUEotNw==`

<Note>Copy the token without leading or trailing spaces.</Note>
</Step>

<Step title="Select events">
Under the **Order Events** and **User Events** sections, enable the event types you want to forward to MoEngage.

<Frame>
  <img src="/guide-media/01KTR68WQRC31JQHEBGD4RRASV" alt="Olo Dashboard showing Order Events and User Events sections for webhook configuration" />
</Frame>
</Step>

<Step title="Save the Shared Secret">
During webhook creation, Olo displays a 64-character **Shared Secret**. Copy and store it securely — Olo displays it only once.

<Warning>
Provide the Shared Secret to your MoEngage contact. MoEngage uses it to verify the `X-Olo-Signature` header on incoming payloads.
</Warning>

{/* TODO: Replace with a MoEngage-hosted screenshot. The image below uses an expiring signed URL and will stop loading. */}
<Frame>
  <img src="https://solink.intercom-attachments-7.com/i/o/874789018/b40997ea1135b81d8edf4015/moUrTi0qcptsb4CTz3DsLYHT9h4EPpTzIR-JFJW1tpqjwMrW8YC0xhrHzJyvmMr-DVMUGI0Bs373eeOray0T8tgDG7q3ZY7djUDniSIfDSb6j71aRvDOoeSr-gN2SAyf0rp26Eo2FgzOo7fOXrN30Qs?expires=1781072100&signature=f5618b1cf453a3a1e9d0060b76bd4178d0da89fc1a1b87b11a2349263a51bd84&req=fCcjEcF3nYBXFb4f3HP0gCvSw393Dv%2B2yK639fop2vP8D0QEcMdNjEcRuEd1%0AdiU%3D%0A" alt="Olo Dashboard showing the Shared Secret field during webhook creation" />
</Frame>
</Step>

<Step title="Test and publish the webhook">
Click **Test Webhook** to send a test payload to your MoEngage endpoint.

<Warning>
The **Publish Webhook** button remains inactive until the MoEngage endpoint returns a `200` response. If the test fails, verify your Destination URL and Authorization header before retrying.
</Warning>

Once the test succeeds, click **Publish Webhook** to activate.
</Step>
</Steps>

<Note>
**Unique identifier**

MoEngage uses Olo's `customerId` as the unique identifier. This value is immutable, ensuring accurate profile resolution and preventing duplicates. To use a different identifier, contact the MoEngage team before configuring your webhook.
</Note>

## Rate limits and reliability

| Topic | Details |
|---|---|
| **Rate limits** | MoEngage limits ingestion of Olo webhook data to a maximum of **500 requests per second (RPS)** per workspace. If your workspace exceeds this limit, MoEngage returns an HTTP `429 (Too Many Requests)` status code. |
| **Retries** | Olo's built-in retry policy handles `429` and `5xx` responses automatically, retrying up to **50 times over a 24-hour period** before dropping the request. |
| **Retry exhaustion** | If Olo exhausts all retry attempts, contact Olo Developer Support at [developersupport@olo.com](mailto:developersupport@olo.com) to replay the failed requests. |
| **Deduplication** | Olo guarantees at-least-once delivery, meaning the same event may arrive more than once under retry conditions. MoEngage uses the `X-Olo-Message-Id` header to deduplicate incoming events automatically. |
